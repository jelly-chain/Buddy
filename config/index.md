# Config plan for Buddy v2 upgrade

This directory contains configuration scaffolds for Buddy v2. All file paths are repo-relative.

- defaults.json: runtime defaults for health checks and chain priorities
- models.json: model configuration and defaults
- forbidden-paths.json: whitelist/deny list for sensitive paths (no secrets in logs/proxied requests)
